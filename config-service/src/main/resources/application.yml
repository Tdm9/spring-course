server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Tdm9/spring-course
          default-label: config-properties
          search-paths:
            - 'greeting-service*'